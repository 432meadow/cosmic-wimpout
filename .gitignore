# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]

# Node / Capacitor, if we wrap the PWA later
node_modules/
ios/App/Pods/

# Simulation build products. results.json and report.txt are kept -- small, and
# they are the data behind the analysis. The HTML and the PDF are generated from
# those by make_report.py, so they do not belong in history.
sim/out/report.html
sim/out/*.pdf
